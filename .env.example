# ============================================================================
# Vertex AI Configuration (PRIMARY - Company Account)
# ============================================================================

# Your company's Google Cloud project ID
VERTEX_AI_PROJECT_ID=your-company-project-id

# Google Cloud region (usually us-central1)
VERTEX_AI_LOCATION=us-central1

# NOTE: Vertex AI uses gcloud authentication, not API keys
# Run: gcloud auth application-default login

# Default model for trace collection
# Options: gemini-pro, claude-sonnet, gemini-flash
DEFAULT_MODEL=gemini-pro

# ============================================================================
# Experiment Configuration
# ============================================================================

EXPERIMENT_NAME=multimodel_collection_v1
RANDOM_SEED=42
LOG_LEVEL=INFO

# ============================================================================
# Data Paths
# ============================================================================

DATA_DIR=./data
RESULTS_DIR=./experiments/results
TRACES_DIR=./data/clean_traces

# ============================================================================
# Collection Settings
# ============================================================================

# Batch size for parallel trace collection
BATCH_SIZE=10

# Retry attempts for failed traces
MAX_RETRIES=3

# ============================================================================
# Model-Specific Settings (Optional)
# ============================================================================

# Gemini Pro settings
GEMINI_TEMPERATURE=0
GEMINI_MAX_TOKENS=2048

# Claude settings
CLAUDE_TEMPERATURE=0
CLAUDE_MAX_TOKENS=2048

# ============================================================================
# Google Colab (Optional - for notebook experiments)
# ============================================================================

GOOGLE_DRIVE_PATH=/content/drive/MyDrive/ai-security-research

# ============================================================================
# Legacy/Alternative APIs (NOT USED - kept for reference)
# ============================================================================

# Only needed if using OpenAI directly (we're using Vertex AI instead)
# OPENAI_API_KEY=sk-your-key-here

# Only needed if using Anthropic directly (we're using Vertex AI instead)
# ANTHROPIC_API_KEY=sk-ant-your-key-here
